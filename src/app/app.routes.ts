import { Routes } from '@angular/router';

/**
 * =========================================================================================
 * Application Routing Table (app.routes.ts) - Modern Angular 22
 * =========================================================================================
 *
 * In Phase 1, the application focuses on component building blocks and template data flow.
 * Feature routes can be defined here as needed using modern `loadComponent` lazy loading syntax.
 */
export const routes: Routes = [];

